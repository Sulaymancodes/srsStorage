<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>srsStorage - Secure Cloud Storage</title>
    <style>
        :root {
            --primary-color: #3b82f6;
            --secondary-color: #60a5fa;
            --background-dark: #0f172a;
            --background-light: #1e293b;
            --text-color: #e2e8f0;
            --text-muted: #94a3b8;
            --card-background: #1e293b;
            --border-color: #334155;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background-color: var(--background-dark);
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 5%;
            background-color: var(--background-light);
            border-bottom: 1px solid var(--border-color);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--primary-color);
            text-shadow: 0 0 10px rgba(59, 130, 246, 0.3);
        }

        .nav-links {
            display: flex;
            gap: 1rem;
        }

        .btn {
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            transition: all 0.3s ease;
        }

        .btn-login {
            color: var(--text-color);
            border: 1px solid var(--border-color);
        }

        .btn-login:hover {
            background-color: var(--background-dark);
        }

        .btn-signup {
            background-color: var(--primary-color);
            color: var(--text-color);
        }

        .btn-signup:hover {
            background-color: var(--secondary-color);
        }

        .hero {
            padding: 4rem 5%;
            background-color: var(--background-dark);
            min-height: calc(100vh - 4rem);
            display: flex;
            align-items: center;
            background: linear-gradient(
                to bottom,
                var(--background-dark),
                var(--background-light)
            );
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
        }

        h1 {
            font-size: 3rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
            text-shadow: 0 0 20px rgba(59, 130, 246, 0.4);
        }

        h2 {
            font-size: 1.5rem;
            color: var(--text-color);
            margin-bottom: 1.5rem;
        }

        .hero-text {
            font-size: 1.125rem;
            max-width: 600px;
            margin: 0 auto 3rem;
            color: var(--text-muted);
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin: 3rem 0;
        }

        .feature {
            background-color: var(--card-background);
            padding: 2rem;
            border-radius: 0.5rem;
            border: 1px solid var(--border-color);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
        }

        .feature:hover {
            transform: translateY(-5px);
        }

        .feature h3 {
            color: var(--secondary-color);
            margin-bottom: 0.5rem;
        }

        .feature p {
            color: var(--text-muted);
        }

        .cta-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 2rem;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: var(--text-color);
            padding: 0.75rem 1.5rem;
            font-weight: bold;
        }

        .btn-primary:hover {
            background-color: var(--secondary-color);
            box-shadow: 0 0 15px rgba(59, 130, 246, 0.4);
        }

        .btn-secondary {
            background-color: transparent;
            color: var(--text-color);
            padding: 0.75rem 1.5rem;
            font-weight: bold;
            border: 2px solid var(--primary-color);
        }

        .btn-secondary:hover {
            background-color: rgba(59, 130, 246, 0.1);
            box-shadow: 0 0 15px rgba(59, 130, 246, 0.2);
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }
            
            h2 {
                font-size: 1.25rem;
            }
            
            .feature-grid {
                grid-template-columns: 1fr;
            }
            
            .cta-buttons {
                flex-direction: column;
            }
            
            .hero {
                padding: 2rem 5%;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">srsStorage</div>
        <div class="nav-links">
            <a href="/log-in" class="btn btn-login">Log in</a>
            <a href="/sign-up" class="btn btn-signup">Sign up</a>
        </div>
    </nav>

    <main class="hero">
        <div class="hero-content">
            <h1>Welcome to srsStorage</h1>
            <h2>Your Personal Secure Storage Solution</h2>
            <p class="hero-text">
                Store, share, and access your files from anywhere with enterprise-grade security.
                Keep your important documents safe and accessible.
            </p>
            <div class="feature-grid">
                <div class="feature">
                    <h3>Secure Storage</h3>
                    <p>Military-grade encryption for all your files</p>
                </div>
                <div class="feature">
                    <h3>Easy Sharing</h3>
                    <p>Share files and folders with custom expiration dates</p>
                </div>
                <div class="feature">
                    <h3>Folder Organization</h3>
                    <p>Keep your files organized with custom folders</p>
                </div>
                <div class="feature">
                    <h3>Quick Access</h3>
                    <p>Access your files from any device, anytime</p>
                </div>
            </div>
            <div class="cta-buttons">
                <a href="/sign-up" class="btn btn-primary">Get Started Free</a>
                <a href="#features" class="btn btn-secondary">Learn More</a>
            </div>
        </div>
    </main>
</body>
</html>